{/* <Container >
        <Grid columns='equal'>
          <Grid.Row>
            <Grid.Column width={8}>
              <Header size='huge' style={{marginTop: '1em'}}>{variants.artist}</Header>
              <Header size='medium' style={{marginTop: '-.5em'}}>{variants.title}</Header>
            </Grid.Column>
            <Grid.Column>
              {albumInfo ? <TrackList trackList={albumInfo.tracklist} /> : null}
            </Grid.Column>
          </Grid.Row>
        </Grid>

    
      </Container> */}